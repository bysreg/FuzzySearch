# 0.3
- fuzzy search now exhaustively searches through all possible matches to find the best heuristic score instead of stopping the search when it finds the first match

# 0.2
- add several more heuristics to fuzzy search

# 0.1
- initial release